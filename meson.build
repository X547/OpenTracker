project('OpenTracker', 'cpp')

cpp = meson.get_compiler('cpp')

dep_be = cpp.find_library('be')
dep_translation = cpp.find_library('translation')

subdir('shared')
subdir('tracker')
subdir('deskbar')
